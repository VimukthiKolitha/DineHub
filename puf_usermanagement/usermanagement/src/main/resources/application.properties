# application.properties

# Application Name
spring.application.name=usermanagement

# MongoDB configuration (adjust the URI as needed)
spring.data.mongodb.uri=mongodb+srv://itpmproject188:PyIx9lRpItndg7ty@cluster0.8ptcv.mongodb.net/userdb
spring.data.mongodb.auto-index-creation=true

# Server port configuration
server.port=8080

# Logging configuration
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.springframework.web=INFO
logging.level.com.example=DEBUG

# Maximum file size for uploads (if handling profile images)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
